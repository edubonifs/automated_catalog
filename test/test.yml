---
- name: h
  hosts: auto
  vars:
    node2: node-2
    node3: node-3
  tasks:

#  - name: g
 #   fetch:
  #    src: hola.txt
   #   dest: .
    #ignore_errors: yes
    #when: node2 == ansible_hostname

  - name: line
    lineinfile:
      path: /etc/hosts
      line: "{{ ansible_all_ipv4_addresses | first }} {{ ansible_fqdn }} {{ ansible_hostname }}"
