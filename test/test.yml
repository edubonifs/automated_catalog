---
- name: h
  hosts: node-4
  tasks:

#  - name: g
 #   fetch:
  #    src: hola.txt
   #   dest: .
    #ignore_errors: yes
    #when: node2 == ansible_hostname
  - name: h
    command: ipa-server-install
