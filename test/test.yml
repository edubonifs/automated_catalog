---
- name: h
  hosts: auto
  vars:
    node2: node-2
    node3: node-3
  gather_facts: no
  tasks:
  - name: g
    fetch:
      src: hola.txt
      dest: .
    ignore_errors: yes

  - name: copy
    copy:
      src: "{{ node3 }}/hola.txt"
      dest: hola.txt
    ignore_errors: yes
